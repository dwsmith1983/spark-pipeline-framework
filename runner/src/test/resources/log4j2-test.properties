# Log4j2 configuration for tests
# Reduces noise during test execution

status = error
name = SparkPipelineTestConfig

# Console appender
appender.console.type = Console
appender.console.name = Console
appender.console.layout.type = PatternLayout
appender.console.layout.pattern = %d{HH:mm:ss.SSS} %-5level %c{1.} - %msg%n

# Root logger - WARN to reduce noise
rootLogger.level = warn
rootLogger.appenderRef.console.ref = Console

# Pipeline framework logs at INFO
logger.pipeline.name = com.example.spark.pipeline
logger.pipeline.level = info

# Suppress Spark noise
logger.spark.name = org.apache.spark
logger.spark.level = warn

logger.hadoop.name = org.apache.hadoop
logger.hadoop.level = error

logger.parquet.name = org.apache.parquet
logger.parquet.level = error

logger.hive.name = org.apache.hive
logger.hive.level = error

logger.jetty.name = org.eclipse.jetty
logger.jetty.level = error
